#Functionality to see if a new descriptor has a match in your database, given the aforementioned cutoff threshold.
#Siri

threshold = "tbd" #find an appropriate threshold later

def get_cosine_distance():
    return #idk once this formula is made


def is_a_match(descriptor):
    if (get_cosine_distance(descriptor, ))



